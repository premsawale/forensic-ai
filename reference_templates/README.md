# Reference Templates

Store trusted/reference layouts for the six supported document types here. These are used later by the layout comparison engine.

Supported types: bonafide, marksheet, fee_receipt, leaving_certificate, degree_certificate, birth_certificate.
